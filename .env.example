DATABASE_URL=postgresql+asyncpg://postgres:pass@localhost:5432/kbk_ti_staging
CRAWLER_EMAIL=your_email@example.com

# Opsional. Kosong = fetcher tetap jalan tanpa key, tapi rate limit Semantic
# Scholar jauh lebih ketat (shared pool publik, sering 429/lambat). Isi kalau
# sudah punya key (gratis, daftar di https://www.semanticscholar.org/product/api)
# untuk rate limit lebih baik (~1 req/detik, lihat config.py).
SEMANTIC_SCHOLAR_API_KEY=
